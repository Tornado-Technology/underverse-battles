event_inherited();

if (sprite_index == start1_animation)
	sprite_index = fight1_animation;
if (sprite_index == end1_animation)
	sprite_index = idle_animation;
if (sprite_index == defeated_animation)
	sprite_index = spr_empty;